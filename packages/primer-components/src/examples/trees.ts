import type { Tree, TreeInteractiveRender } from "@hackworthltd/primer-types";

/*
Some example trees.

It's important that node IDs are unique across all these trees,
as we'd like to render them simultaneously on one canvas.
*/

// @ts-expect-error
export const tree1: Tree = undefined;

// @ts-expect-error
export const tree2: Tree = undefined;

// @ts-expect-error
export const tree3: Tree = undefined;

// @ts-expect-error
export const tree4: Tree = undefined;

// @ts-expect-error
export const tree5: TreeInteractiveRender = undefined;
